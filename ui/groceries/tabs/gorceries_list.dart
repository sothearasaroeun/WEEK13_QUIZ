import 'package:flutter/material.dart';

class GorceriesList extends StatelessWidget {
  const GorceriesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}