import 'package:flutter/material.dart';

class SearchGorcery extends StatelessWidget {
  const SearchGorcery({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}